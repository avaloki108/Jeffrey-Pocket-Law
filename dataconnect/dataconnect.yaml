specVersion: "v1"
serviceId: "jeffery-friendly-pocket-law-4-service"
location: "us-east4"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "jeffery-friendly-pocket-law-4-database"
      cloudSql:
        instanceId: "pocket-lawyer-fdc"
        # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
        # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./example"]
