version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: jeffrey-qdrant
    ports:
      - "6333:6333"  # REST API
      - "6334:6334"  # gRPC API
    volumes:
      - ./qdrant_storage:/qdrant/storage
    environment:
      # Enable CORS for local development
      - QDRANT__SERVICE__HTTP_PORT=6333
      - QDRANT__SERVICE__GRPC_PORT=6334
    restart: unless-stopped
    networks:
      - jeffrey-network

networks:
  jeffrey-network:
    driver: bridge

# Optional: Add Qdrant Web UI for easier management
# Uncomment the section below to enable the UI at http://localhost:8080
#  qdrant-ui:
#    image: qdrant/qdrant-web-ui:latest
#    container_name: jeffrey-qdrant-ui
#    ports:
#      - "8080:80"
#    environment:
#      - QDRANT_HOST=qdrant
#      - QDRANT_PORT=6333
#    depends_on:
#      - qdrant
#    networks:
#      - jeffrey-network